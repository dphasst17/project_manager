import { AlertDialogContent } from "@/components/ui/alert-dialog"

const BudgetDialog = ({closeDialog}:{closeDialog:() => void}) => {
  return <AlertDialogContent></AlertDialogContent>
}
export default BudgetDialog
